const byId = (id) => document.getElementById(id);
let appState = { events: [], scan: null, status: null, sport: "All", running: false };
function text(id, value) { const el = byId(id); if (el) el.textContent = value; }
function escapeHtml(value) { const div = document.createElement("div"); div.textContent = value == null ? "" : String(value); return div.innerHTML; }
async function api(url, options) { const controller = new AbortController(); const timer = setTimeout(() => controller.abort(), 15000); try { const response = await fetch(url, Object.assign({cache:"no-store",signal:controller.signal}, options||{})); const body=await response.text(); let data; try{data=JSON.parse(body);}catch(_){throw new Error(url+" returned invalid JSON ("+response.status+")");} if(!response.ok)throw new Error((data&&data.detail)||url+" returned "+response.status); return data;} catch(error){if(error&&error.name==="AbortError")throw new Error(url+" timed out after 15s");throw error;} finally{clearTimeout(timer);} }
function showView(name){document.querySelectorAll(".view").forEach(el=>el.classList.toggle("active",el.dataset.page===name));document.querySelectorAll(".appNav a").forEach(el=>el.classList.toggle("active",el.dataset.view===name));const titles={dashboard:"Edge Command Center",scanner:"Live Scanner",multi:"Today's Multi",results:"Results",analytics:"Analytics"};text("pageTitle",titles[name]||"MultiSport Edge AI");window.scrollTo(0,0);}
document.querySelectorAll(".appNav a").forEach(el=>el.addEventListener("click",event=>{event.preventDefault();showView(el.dataset.view);}));
function eventHtml(event){const score=event.home_score!=null&&event.away_score!=null?event.home_score+" – "+event.away_score:(event.status||event.time||"Scheduled");return '<div class="fixture"><div><b>'+escapeHtml(event.event)+'</b><span>'+escapeHtml(event.sport)+' · '+escapeHtml(event.league||"")+'</span></div><time>'+escapeHtml(score)+'</time></div>';}
function renderEvents(data){appState.events=data.events||[];text("overviewEvents",data.count||0);text("fixtureCount",appState.events.length+" events");const fixtures=byId("fixtures");if(fixtures){fixtures.className=appState.events.length?"":"empty";fixtures.innerHTML=appState.events.length?appState.events.map(eventHtml).join(""):"No verified events returned today.";}const finished=appState.events.filter(x=>x.home_score!=null&&x.away_score!=null);text("resultCount",finished.length+" scored");const results=byId("resultsBox");if(results){results.className=finished.length?"":"empty";results.innerHTML=finished.length?finished.map(eventHtml).join(""):"No completed/scored events returned yet.";}}
function renderScan(scan){appState.scan=scan;const observed=Number(scan.observed_records||0);const counts=scan.bookmaker_counts||{};const sportingbet=Number(counts.sportingbet||0);const betway=Number(counts.betway||0);const qualified=Number(scan.qualified_records||0);const portfolio=scan.portfolio||{};text("overviewSportingbet",sportingbet);text("overviewBetway",betway);text("overviewQualified",qualified);text("overviewPortfolio",portfolio.status==="QUALIFIED_PORTFOLIO"?"Ready":"None");text("legs",portfolio.selected_count||0);text("odds",portfolio.combined_odds||"—");text("confidence","≥90");if(portfolio.status==="QUALIFIED_PORTFOLIO"){text("multiStatus","Today's portfolio is ready");text("multiReason",(portfolio.selected_count||0)+" qualified legs · combined odds "+portfolio.combined_odds);}else{text("multiStatus",observed?"Odds collected — qualification pending":"Bookmaker odds feed degraded");text("multiReason",observed?("Sportingbet "+sportingbet+" · Betway "+betway+" · "+qualified+" pass ≥90."):"Fixtures can operate independently; verified bookmaker odds are currently unavailable.");}}
function renderStatus(status){appState.status=status;const dot=byId("providerDot");if(dot)dot.className=status.ok?"ok":"bad";text("providerText",status.degraded?"Events live · odds feed degraded":"Pipeline live");if(status.time_sast)text("lastUpdated",new Date(status.time_sast).toLocaleTimeString());text("qualityBox",status.degraded?(status.degraded_reason||"A pipeline component is degraded."):"Bookmaker observations are stored separately and the evidence/model gate remains downstream.");}
function showBootError(message){const dot=byId("providerDot");if(dot)dot.className="bad";text("providerText","System check failed");text("multiStatus","Dashboard connected · provider check failed");text("multiReason",message);text("qualityBox",message);}
async function refresh(full){if(appState.running)return;appState.running=true;const button=byId("scanBtn");if(button){button.disabled=true;button.textContent="Refreshing…";}text("providerText","Checking system…");try{if(full){try{await api("/v1/market-collector/run",{method:"POST"});}catch(error){console.warn("Collector:",error);}}const settled=await Promise.allSettled([api("/v1/web-intelligence/scan"),api("/v1/events/today"),api("/v1/system/status")]);let successes=0;if(settled[0].status==="fulfilled"){renderScan(settled[0].value);successes++;}if(settled[1].status==="fulfilled"){renderEvents(settled[1].value);successes++;}if(settled[2].status==="fulfilled"){renderStatus(settled[2].value);successes++;}if(!successes)throw new Error(settled.map(x=>x.status==="rejected"?x.reason.message:"").filter(Boolean).join(" · "));if(!appState.status)text("providerText",successes+"/3 services responding");}catch(error){showBootError(error&&error.message?error.message:"Unknown startup error");}finally{appState.running=false;if(button){button.disabled=false;button.textContent="Run full refresh";}}}
window.addEventListener("error",event=>showBootError("UI error: "+event.message));window.addEventListener("unhandledrejection",event=>showBootError("Request error: "+(event.reason&&event.reason.message?event.reason.message:event.reason)));const scanButton=byId("scanBtn");if(scanButton)scanButton.addEventListener("click",()=>refresh(true));text("providerText","Connecting…");refresh(false);setInterval(()=>refresh(false),120000);
